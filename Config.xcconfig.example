// Copy this file to `Config.xcconfig` and set your TMDB bearer token.
// Or create `.env` and run `pod install` — the token is synced automatically.
TMDB_BEARER_TOKEN = your_tmdb_read_access_token_here
